function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(46, 204, 113); // 画面を緑色にする
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(32);
  text("GitHub連携テスト成功！", width/2, height/2);
}
